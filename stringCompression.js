// 1.5// Implement a method to perform basic string compression using the counts of repeated characters.// For example, the string aabcccccaaa would become a2blc5a3.// If the "compressed" string would not become smaller than the original string, your method should return the original string.var compressString = function(string) {  var compressed = '';  var currentChar = string[0];  var currentCount = 1;  for (var i = 1; i < string.length; i++) {    if (string[i] === currentChar) {      if (i === string.length - 1) {        currentCount++;        compressed += currentChar + currentCount;      }      currentCount++;    } else {      compressed += currentChar + currentCount;      currentChar = string[i];      currentCount = 1;    }  }  if (compressed.length < string.length) {    return compressed;  } else {    return string;  }};console.log(compressString('aabbccdd')); // aabbccddconsole.log(compressString('aabbccddd')); // a2b2c2d3console.log(compressString('woooooooo yeyyyyyyyyy')); // w1o8 1y1e1y9