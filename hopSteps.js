// A child is running up a staircase with n steps,// and can hop either 1 step, 2 steps, or 3 steps at a time.// Implement a method to count how many possible ways the child can run up the stairs.function countHopPermutations(steps) {  var count = 0;  function generateHopPermutations(steps, currentStep) {    currentStep = currentStep || 0;    if (currentStep === steps) {      return ++count;    } else if (currentStep > steps) {      return;    } else {      for (var i = 1; i <= 3; i++) {        generateHopPermutations(steps, currentStep + i);      }    }  }  generateHopPermutations(steps);  return count;}console.log(countHopPermutations(5));